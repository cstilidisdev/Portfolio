// ---- This code is to be placed to anothe file

// import { add, multiply, PI } from "./module.js";

// console.log(add(2, 3));

export function add(x, y) {
  return x + y;
}

export function multiply(x, y) {
  return x * y;
}

export const PI = 3.14159;
